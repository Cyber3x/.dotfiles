# Overrides for the work PC.
# NOTE: the filename must match the output of `hostname` on that machine.
# Anything can go here (aliases, exports, ...), it loads after the shared
# files and before bash_prompt.sh.

C_USER='\[\e[38;5;39;1m\]'  # blue
C_PATH='\[\e[38;5;114;1m\]' # green
